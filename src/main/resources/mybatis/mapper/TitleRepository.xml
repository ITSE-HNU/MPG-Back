<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ghstudio.pairprogram.dao.repository.TitleRepository">
    <resultMap id="TitleMap" type="com.ghstudio.pairprogram.dao.entity.Title">
        <id property="id" column="id"/>
        <result property="question" column="question"/>
        <result property="choiceA" column="choice_A"/>
        <result property="choiceB" column="choice_B"/>
        <result property="choiceC" column="choice_C"/>
        <result property="choiceD" column="choice_D"/>
        <result property="correct" column="correct"/>
    </resultMap>

    <select id="queryTitles" resultMap="com.ghstudio.pairprogram.dao.repository.TitleRepository.TitleMap">
        SELECT * FROM titles t
        <where>
            <if test="params != null">
                t.id in
                <foreach collection="params" item="id" open="(" close=")" separator=",">
                    #{id}
                </foreach>
            </if>
        </where>
    </select>
</mapper>
